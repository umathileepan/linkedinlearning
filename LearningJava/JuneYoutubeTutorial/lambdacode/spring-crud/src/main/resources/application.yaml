server:
  port: 8081
  error:
    include-message: always

spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/user_db
    password: Astro20
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true


